import { Router } from 'express';

const authRouter = Router();

authRouter.post('/verify-session', );
authRouter.post('/signin', );
authRouter.post('/signup', );
authRouter.post('/signout', );

export default authRouter;
